name: Web CI

on:
  pull_request:
    branches:
      - develop
      - main
  push:
    branches:
      - develop
      - main

permissions:
  contents: read

concurrency:
  group: web-ci-${{ github.workflow }}-${{ github.ref }}
  cancel-in-progress: true

jobs:
  verify:
    name: Lint and build
    runs-on: ubuntu-latest
    timeout-minutes: 10
    env:
      AUTH_SECRET: marketpilot-ci-build-secret
      AUTH_GOOGLE_ID: marketpilot-ci-google-id
      AUTH_GOOGLE_SECRET: marketpilot-ci-google-secret
      MARKETPILOT_API_URL: http://127.0.0.1:8000
      MARKETPILOT_INTERNAL_API_TOKEN: marketpilot-ci-internal-token
      MARKETPILOT_USER_API_SIGNING_SECRET: marketpilot-ci-signing-secret

    defaults:
      run:
        working-directory: apps/web

    steps:
      - name: Check out repository
        uses: actions/checkout@v4

      - name: Set up Node.js
        uses: actions/setup-node@v4
        with:
          node-version-file: .nvmrc
          cache: npm
          cache-dependency-path: apps/web/package-lock.json

      - name: Install dependencies
        run: npm ci

      - name: Run ESLint
        run: npm run lint

      - name: Build application
        run: npm run build
